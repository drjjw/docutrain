node_modules/
.env
.env.local
*.log
.DS_Store
dist/

# Local embedding model cache
.cache/

# Database dumps
database-dumps/

# Backup files - exclude all backups
backups/supabase_backup_*/
backups/*.backup
backups/supabase_backup_latest
db_cluster-*.backup

# PDFs (used only for training/embedding, not needed in git)
PDFs/

# Supabase local config
supabase/.env.local
supabase/.temp/
*.pid
